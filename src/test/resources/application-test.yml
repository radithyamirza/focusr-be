spring:
  datasource:
    url: jdbc:h2:mem:focusr_test;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect

app:
  jwt:
    secret: testsecretkey_which_is_long_enough_for_hmac256_atleast
    expiration-ms: 86400000
  cors:
    allowed-origin: http://localhost:3000
